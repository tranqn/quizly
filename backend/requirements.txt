# Web framework & REST API
Django==6.0.6
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
python-dotenv==1.2.2

# Quiz generation pipeline
# yt-dlp downloads the YouTube audio, openai-whisper transcribes it locally
# (requires FFmpeg installed globally), and google-genai prompts Gemini Flash.
# Note: openai-whisper pulls in PyTorch and needs a Python version with
# available wheels (3.10-3.12 recommended).
yt-dlp==2025.7.21
openai-whisper>=20250625
google-genai>=1.0.0
